<Window x:Class="grzyClothTool.Views.SplashScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:grzyClothTool.Views"
        mc:Ignorable="d"
        Title="SplashScreen"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        Height="420"
        Width="420"
        Background="Transparent"
        >
    <Border Background="{DynamicResource Brush50}" CornerRadius="28" BorderThickness="2" BorderBrush="{DynamicResource AccentBrush}">
        <Grid>
            <Grid.RowDefinitions >
                <RowDefinition Height="*" />
                <RowDefinition Height="50" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0">
                <TextBlock x:Name="x" Padding="5" HorizontalAlignment="Center" FontSize="42" FontWeight="SemiBold" VerticalAlignment="Bottom" Foreground="{DynamicResource Brush950}" Text="YabosClothTool" />
                <Image Width="270" Source="pack://application:,,,/Resources/YabosClothTool.png" />
            </StackPanel>
            <StackPanel Grid.Row="1" >
                <TextBlock x:Name="updateTextBox" Padding="5" HorizontalAlignment="Center" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Bottom" Foreground="{DynamicResource Brush800}" Text="Loading..." />
                <ProgressBar Background="{DynamicResource Brush150}" Foreground="{DynamicResource AccentBrush}" BorderThickness="0" x:Name="progressBar" HorizontalAlignment="Center" VerticalAlignment="Center" Width="300" Height="15" IsIndeterminate="True">
                    <ProgressBar.Clip>
                        <RectangleGeometry RadiusX="5" RadiusY="5" Rect="0,0,300,10"/>
                    </ProgressBar.Clip>
                </ProgressBar>
                
            </StackPanel>
        </Grid>
        
    </Border>
</Window>
